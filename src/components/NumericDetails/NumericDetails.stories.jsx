import { NumericDetails } from './index';

export default {
  title: 'General/NumericDetails',
  component: NumericDetails,
};

const Template = (args) => <NumericDetails {...args} />;

export const Default = Template.bind({});
