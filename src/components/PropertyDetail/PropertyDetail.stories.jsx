import { PropertyDetail } from "./index";

export default {
  title: "Zappy/PropertyDetail",
  component: PropertyDetail,
};

const Template = (args) => <PropertyDetail {...args} />;

export const Default = Template.bind({});
